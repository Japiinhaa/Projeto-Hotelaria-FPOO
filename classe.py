class Hotel:

    class Quartos:
        def __init__(self, numero, preco, capacidade, movel):
            self.numero = numero
            self.capacidade = capacidade
            self.preco = preco
            self.alugado = False

def mostrar_detalhes(self):
    print(f"Quarto {self.numero} - Capacidade: {self.capacidade} pessoas - Preço: R${self.preco:} por noite - Movéis: {self.movel} Móveis")
    if self.alugado:
        print("Status: Alugado")
        else:
        
        print("Status: Disponível")

        def reservar(self):
              if not self.reservar:
                    self.reservar = True
                    print(f"Quarto {self.reservar} alugado com sucesso!!")
                else:
                    
                    print(f"O Quarto {self.alugado} está ocupado")

# listar quartos


class AP_luxo(Quartos):
        def __init__(self, numero, capacidade, preco,moveis ):
              
              
    class AP_master(Quartos):
        def __init__(self, numero, capacidade, preco,moveis ):


class AP_simples(Quartos):
        def __init__(self, numero, capacidade, preco,moveis ):


class AP_simples_casal(Quartos):
    def __init__(self, numero, preco, capacidade, moveis):
        super().__init__(numero, preco, capacidade, moveis)


class AP_duplo(Quartos):
      def __init__(self, numero, capacidade, preco,moveis ):


class AP_duplo_casal(Quartos):
    def __init__(self, numero, capacidade, preco,moveis ):